import './App.css';

type AppProps = {}

export const App = ({ }: AppProps) => {
  return (
    <div className="App">
      <header className="App-header">
        <p>My Portfolio</p>
        <p>Have problems with branches</p>
        <p>Testing the branches with the new migration</p>
      </header>
    </div>
  );
}
